// build a 100x dev community platform easy to use and work with. only for cohort students  

set up ui by tommorrow and write all ideas down regarding creating it a best platform add a best ai layer above it

--> set up taking user perferences and save it in db and align setup your account button to left sidebar instead of between
--> set up profile tab and login logout button correctly


prisma commands
--> npm exec prisma migrate dev (new migration file)
--> npx prisma generate